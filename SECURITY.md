# 安全说明

## 敏感信息

不要把真实密钥、数据库密码、JWT 密钥、第三方 API Key 提交到代码仓库。

推荐把敏感配置放到本地 `.env` 或部署平台的环境变量中。

## 鉴权

项目使用 JWT 做用户鉴权。所有需要登录的接口都应该通过 `Authorization: Bearer <token>` 传入用户 token。

## 日志安全

生产日志中不要记录密码、完整 token、API Key、银行卡号等敏感信息。必要时只记录脱敏后的片段。

## 数据隔离

会话、聊天记录和长期记忆都应该按 `user_id` 做隔离，避免用户之间互相读取数据。

## 依赖安全

升级依赖前建议运行：

```bash
uv run ruff check .
uv run pyright app
```

生产部署前也建议检查依赖漏洞和镜像漏洞。
